We are going to run code locally via Docker Compose. 

1. We will use Ollama for local LLM generation (llama3)
2. Qdrant running locally for vector storage
3. FastAPI for our service layer
4. Sentence-Transformers for generating embeddings locally without relying on any paid APIs.